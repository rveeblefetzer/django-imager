language: python
python:
  - "2.7"
  - "3.5"

services:
  - postgresql

before_script:
  - psql -c "create database test_imager_db;" -U postgres

# command to install dependencies
install:
  - pip install -r requirements.pip

# command to run tests
script: 
  - python manage.py test

after_success:
  - coveralls